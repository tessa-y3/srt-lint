[project]
name = "srtlint"
version = "0.1.0"
description = "Validating parser and pretty printer for SRT subtitle files"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
authors = [{ name = "tessa-y3" }]
dependencies = []

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"
